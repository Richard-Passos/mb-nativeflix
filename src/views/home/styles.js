import { StyleSheet } from "react-native";

const paddingTop = 16;

export default StyleSheet.create({
  container: {
    paddingTop,

    flex: 1,
  },
  carousels: {
    marginVertical: 24,

    gap: 24,
  },
  height: {
    height: paddingTop,
  },
});
