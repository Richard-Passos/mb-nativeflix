import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    borderWidth: 1,
    padding: 16,

    alignItems: "center",
    gap: 24,
  },
  icons: {
    flexDirection: "row",
    gap: 24,
  },
});
